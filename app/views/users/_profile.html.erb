<!--<h4 class="text-2xl"><%= @user.name %></h4>-->

<% if current_user == @user %>


<!--左側のカラム-->
<div class="mt-10">

<!--username-->
<p class="text-gray-500 pl-6"><%= @user.name %></p>

<button class="ml-3 mt-3">
  <%= link_to root_path, class: "px-2 py-1 text-lg font-semibold flex items-center space-x-2" do %>
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
      <path stroke-linecap="round" stroke-linejoin="round" d="m2.25 12 8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
    </svg> ホーム
  <% end %>
</button>

<!--postボタン-->
<div class="">
  <div class="bg-gray-800 m-1 p-2 rounded">
   <p class="text-xl text-white font-semibold pl-4">
   <%= link_to "Post", current_user ? zen_path(current_user) : login_path, class: "font-bold text-white no-underline", data: { turbo: false } %>
   </p>
  </div>
</div>


<!--<button class="ml-3 mt-3">
  <%= link_to root_path, class: "px-2 py-1 text-lg font-semibold flex items-center space-x-2" do %>
    レベル: 🌱
  <% end %>
</button>-->


<!-- スポンサーページ -->
<div class="flex items-center pl-1 fixed bottom-10">
  <!-- eigopencil -->
  <div class="rounded">
    <%= link_to "https://eigopencil.com" do %>
      <%= image_tag "ep.png", class: "h-12 object-contain" %>
    <% end %>
  </div>

  <!-- ホステル日記 -->
  <div class="rounded">
    <%= link_to "https://readhostel.vercel.app/" do %>
      <%= image_tag "hostel.png", class: "h-10 object-contain" %>
    <% end %>
  </div>
</div>
<!-- スポンサーページ -->

<fotter class="fixed bottom-0 text-gray-500">
© 2025 katakata.fun 
</fotter>


<% 
=begin
%>
<!-- tags -->
<%= link_to root_path do%>
  <p class="mt-5 px-2 py-1">🔖</p>
  <p class="items-center px-2 py-1 text-medium font-medium">
  全て
  </p>
 <% end %>

<% tagsArr = [] %>
<% @microposts.each do |micropost| %>
 <% tagsArr.push(micropost.tags) %>
<% end %>


<% uniqtags = tagsArr.to_set %>

<% uniqtags.each do |tag| %>

<% if tag.present? %>
 
 <%= link_to tag_path(tags: tag) do%><!--動的linkを生成するときに、routerで定義した:tagsを渡してあげる必要がある。paramsはcontrollerのみへの受け渡しなので使われない。https://railsdoc.com/page/link_to-->
 
 <% if params[:tags] == tag %> 
  <p class="tag items-center px-2 py-1 text-medium font-medium text-white">
  # <%= tag %>
  </p>
 <% else %>
  <p class="tag items-center px-2 py-1 text-medium font-medium text-gray-600">
  # <%= tag %>
  </p>
<% end %>

<% end %>

<% end %>

<% end %>
<!-- tags -->
<%
=end
%>


</div>




<% end %>


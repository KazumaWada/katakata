<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>katakata</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans JP', sans-serif;
        }
    </style>
</head>
<body class="">

    <!-- Navigation -->
    <nav class="top-0 left-0 right-0 bg-white z-50 h-16">
    <div class="container mx-auto px-4 h-full flex justify-between items-center">
      <div class="flex items-center space-x-3 h-full">
        <a href="/" class="flex items-center text-base font-bold text-gray-800">
          <%= image_tag "favicon.ico", class: "h-10 object-contain" %>atakata.fun
        </a> 
      </div>
      <!--<div class="space-x-4">
        <%= link_to login_path, data: { turbo: false }, class: "bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition" do %>
          はじめる
        <% end %>
      </div>-->
    </div>
  </nav>

  <!-- hero section -->
  <!-- 下線: after:block after:h-0.5 after:bg-gray-300 after:opacity-50 after:mt-1 after:w-full -->
  <header class="w-full h-screen flex items-start justify-center pt-20 bg-gray-800 leading-relaxed text-white">
  <div class="container mx-auto px-4 flex flex-col md:flex-row items-center gap-10">

    <!-- 左側：テキスト -->
    <div class="text-center md:text-left max-w-xl">
      <h1 class="text-4xl sm:text-5xl font-extrabold">
      カタカタ打つ、今日も少し、英語が育つ🌱
      </h1>
      
      <div class="tags mt-10">
      <p>英語日記で、一歩ずつ理想の自分へ。</p>

      <div class="pt-10">
       <%= link_to login_path, data: { turbo: false }, class: "bg-gray-900 text-white hover:bg-white hover:text-gray-900 text-xl px-6 py-3 rounded-lg transition font-bold" do %>
       はじめる
       <% end %>
      </div>

  
    
      </div>
    </div>

    <!-- 右側：モックアップ画像 -->
    <div class="flex flex-col md:flex-row items-center gap-6 sm:p-10">
      <div class="relative w-[400px] md:w-[500px] rounded-2xl">
        <%= image_tag "katakata.jpg", class: "w-full object-cover rounded-2xl" %>
        <div class="absolute inset-0 bg-gradient-to-r from-transparent to-white opacity-30 pointer-events-none"></div>
      </div>
    </div>
    

  </div>
</header>






    <!-- Footer -->
    <footer class="py-4 border-solid border-t-2">
        <div class="container mx-auto px-4 text-center">
            <p>&copy; <%= Date.current.year %> katakata. All rights reserved.</p>
        </div>
    </footer>
</body>

</html>


<script>
function showImage(type) {
  // まず全画像を非表示にする
  document.querySelectorAll('#image-container img').forEach(img => {
    img.classList.add('hidden');
  });

  // 指定された画像だけ表示する
  const target = document.getElementById(`img-${type}`);
  if (target) {
    target.classList.remove('hidden');
  }
}
</script>
